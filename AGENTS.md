# 워크플로우

명시적으로 요청한 경우가 아니라면 brainstorming, plan 관련 스킬을 사용하지 않는다.
구현을 요청받기 전에 설계 및 계획 문서를 준비한다.

- 설계 문서와 계획 문서가 이미 존재하면 해당 문서를 재사용하고 1~4단계를 생략한다.
- 문서가 없거나 누락된 경우에만 다음 단계를 진행한다.

1~4 단계를 진행할 때, 이슈의 Label에 Cross-Repo가 설정되어 있다면 프론트와 백엔드를 모두 고려하여 문서를 작성한다.
1. `brainstorming`을 통해 요구사항과 설계를 정리하고 설계 문서를 작성한다.
2. 설계 문서를 Linear 플러그인을 통해 연관된 Issue의 `Resources`에 등록한다.
3. `writing plans`를 통해 구현 계획 문서를 작성한다.
4. 계획 문서를 Linear 플러그인을 통해 동일한 연관 Issue의 `Resources`에 등록한다.

설계 및 계획 문서가 준비된 후 사용자가 구현을 명시적으로 요청하면 다음을 수행한다.

5. Linear 플러그인으로 연관된 Issue를 확인하고 상태를 `In progress`로 변경한다.
6. 설계 문서와 계획 문서를 기준으로 구현을 완료한다.
7. 사용자가 커밋 또는 병합을 명시적으로 요청하면 부모 Issue에 연결된 모든 sub-issue가 해결되었는지 확인한다.
8. 해결되지 않은 sub-issue가 하나라도 있으면 커밋 또는 병합을 수행하지 않고, 미해결 sub-issue가 남아 있음을 사용자에게 알린다.
9. 8번을 만족하지 않음에도 사용자가 커밋 또는 병합을 요청하는 경우, 추후 issue를 해결해야 함을 명시하는 상태로 바꾸고 안전하게 커밋 또는 병합을 진행한다.
10. Issue와 관련된 작업이 완료되면 연관된 Issue의 상태를 `Done`으로 변경한다.

# 테스트 제외
1. 프론트엔드는 절대로 테스트를 작성하지 않는다.
2. 1번은 사용자가 명시적으로 테스트를 요청하는 경우는 제외한다.

# 테스트 문서 작성 규칙

테스트 시나리오·수동 QA 문서는 실무 테스트 케이스 형식과 BDD의 Given–When–Then 구조를 결합하여 작성한다.

- 테스트 문서를 작성하기 전에 부모 Linear Issue에 등록된 설계 문서 및 계획 문서의 테스트 케이스와 Verification Checklist를 먼저 확인한다.
- 설계와 계획 문서에 정의된 테스트 시나리오를 단일 기준으로 사용한다. 시나리오를 임의로 이름 변경·생략·대체하지 않는다.
- 문서에 없는 추가 검증이 필요하면 `보조 검증`으로 명시하고, 기존 시나리오와 구분한다.
- 문서 목적과 적용 범위를 먼저 정의한다.
- 테스트 환경, 사전조건, 테스트 계정·데이터, 외부 의존성을 명시한다.
- 탈퇴·삭제·결제처럼 되돌릴 수 없는 동작은 테스트 전 기준선과 데이터 정리 주의사항을 포함한다.
- 각 테스트 케이스에 고유 ID를 부여하고 다음 항목을 포함한다.
  - `Given`: 사전조건
  - `When`: 수행 절차
  - `Then`: 기대 결과와 판정 기준
  - `Evidence`: 화면, HTTP 응답, 로그, 데이터베이스 query, 외부 provider 상태 등 검증 증거
- 테스트 결과는 `Pass`, `Fail`, `Blocked`, `Not Run` 중 하나로 기록할 수 있어야 한다.
- 실패 또는 차단 시 재현 절차, 실제 결과, 관련 증거, 후속 Issue를 함께 기록한다.
- Cross-Repo 이슈는 프론트엔드·백엔드·데이터베이스·Queue·외부 provider 등 관련 경계를 모두 포함한다.
- 부모 Linear Issue가 있으면 테스트 문서는 반드시 부모 Issue의 `Resources`에 등록한다. 테스트 대상 sub-issue가 아니라 부모 Issue에 문서 링크와 적용 범위를 comment로 남긴다.

